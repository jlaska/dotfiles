# Credentials and secrets
*.key
*.pem
*.p12
*.pfx
*.gpg
*.asc
*_rsa
*_rsa.pub
*_ed25519
*_ed25519.pub
*.token
*.secret
.env
.env.*
!.env.example

# GnuPG runtime/key material (only .conf files are tracked)
*.kbx
*.kbx~
random_seed
S.gpg-agent*
private-keys-v1.d/

# SSH key material and runtime files (only config is tracked)
known_hosts*
authorized_keys

# GitHub CLI auth (only config.yml is tracked)
hosts.yml

# Machine-local overrides
*.local
*.local.json
*.local.yaml
*.local.yml
settings.local.*

# Runtime and application state
*.log
*.lock
cache/
sessions/
history.*
*.pid
*.sock

# Auto-provisioned content
plugins/
pack/
bundle/
vendor/
node_modules/

# OS artifacts
.DS_Store
Thumbs.db
._*
.Spotlight-V100
.Trashes

# Editor artifacts
*.swp
*.swo
*~
.idea/
.vscode/
*.orig

# Vim runtime files
*.spl
.netrwhist
